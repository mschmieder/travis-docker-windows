os: windows
language: bash
services: docker

script:
  - docker version
  - docker images
  - docker info
  - cd c:/ & dir
  - powershell -Command & $Env:ProgramFiles\Docker\Docker\DockerCli.exe -SwitchDaemon .
