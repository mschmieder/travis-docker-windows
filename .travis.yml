os: windows
language: bash
services: docker

script:
  - docker version
  - docker images
  - docker info
  - choco install -y wsl-ubuntu-1804
  - wsl --list --all
  - wsl --list --running
  - wslconfig /?
  - wslconfig /list /all
